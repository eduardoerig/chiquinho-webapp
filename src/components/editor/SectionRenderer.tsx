"use client";

/**
 * SectionRenderer — versão simplificada.
 * O componente principal de renderização é DynamicSections.tsx.
 * Este módulo é mantido apenas como re-export para compatibilidade.
 */
export { DynamicSections as SectionRenderer } from "@/components/layout/DynamicSections";
